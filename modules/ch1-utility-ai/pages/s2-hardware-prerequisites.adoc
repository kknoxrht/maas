:time_estimate: 5

= Hardware & Platform Prerequisites

_Estimated reading time: *{time_estimate} minutes*._

[.lead]
*MaaS is the wiring, but generic GPU enablement is the foundation—without certified accelerators and proper drivers, the entire stack fails.*

Objective::
Identify the hardware and platform requirements needed before enabling Models-as-a-Service on Red Hat OpenShift AI.

== The Foundation: GPU Enablement

Before configuring MaaS, your OpenShift cluster must have:

Certified Accelerators::
Data center grade GPUs from NVIDIA (Turing/Ampere/Hopper/Blackwell), AMD (CDNA), or IBM Spyre

Node Feature Discovery Operator::
NFD operator to detect and label GPU nodes

GPU Operator::
NVIDIA GPU Operator (or equivalent) managing driver containers

GPU Drivers (Policy)::
Properly installed and verified CUDA or ROCm drivers

A platform engineer thinking "We'll just enable MaaS" faces a hard stop without this foundation. The MaaS layer assumes the underlying GPU infrastructure is already working; it orchestrates access to the models, not the hardware itself.

== Required Platform Components

MaaS requires Red Hat OpenShift AI 3.3 or later with these dependencies:

[source,yaml]
----
Required Operators:
- Red Hat OpenShift AI operator
- Red Hat Connectivity Link (Kuadrant)
- cert-manager operator
- Gateway API operator
- LeaderWorkerSet operator
- Llama Stack operator (for dashboard UI)
----

Missing any of these causes deployment failures. The error messages can be cryptic:

[source,bash]
----
$ oc get datasciencecluster
NAME    PHASE    AGE
maas    Failed   2m
----

== Hardware Certification Requirements

Not all GPUs are supported. MaaS requires:

[options="header"]
|===
| Vendor | Accelerator Models | CPU Architecture Support
| NVIDIA | Turing: T4 +
Ampere: A2, A10, A16, A30, A40, A100 +
Ada Lovelace: L4, L20, L40, L40S +
Hopper: H100, H200, H20, GH200 +
Blackwell: GB200, GB300, B200, B300, RTX PRO 6000 Blackwell Server Edition, RTX PRO 4500 Blackwell Server Edition | x86, AArch64
| AMD | Instinct MI210, Instinct MI300X, Instinct MI325X | x86
| Google | v4, v5e, v5p, v6e (Trillium) | x86
| IBM | Spyre for Power | IBM Power (ppc64le)
| IBM | Spyre for Z | IBM Z (s390x)
| IBM | AIU | x86
| AWS | Inferentia2 (Inf2), Trainium (Trn1, Trn1n, Trn2) | x86
|===


Consumer GPUs Not Supported::
Gaming cards (RTX 3090, RTX 4090) lack the reliability and driver support needed for production inference

== Verification Checklist

Before enabling MaaS, verify:

- [ ] GPU nodes are labeled and scheduled properly (NFD / GFD)
- [ ] CUDA/ROCm drivers are working (`nvidia-smi` or `rocm-smi` succeeds)
- [ ] Required operators are installed and healthy
- [ ] OpenShift Container Platform is version 4.20+
- [ ] Red Hat OpenShift AI is version 3.3+

== Common Pitfalls

**Problem**: Models fail to publish as MaaS endpoints +
**Cause**: Gateway API operator missing +
**Solution**: Verify all dependency operators are present

**Problem**: DSC failed to declare MaaS Component ready +
**Cause**: MaaS Gateway API endpoint must exist +
**Solution**: The OpenShift AI Operator looks for a specific Gateway resource (maas-default-gateway). If it doesn’t exist, it halts the MaaS API deployment to prevent unroutable services. Create the gateway

== What's Next

Now that you understand the prerequisite hardware and platform requirements, the next section maps the MaaS architecture stack—showing how KServe, Kuadrant, and Gateway API work together to deliver centralized model serving.

////
Section Notes:
- Based on design doc Module 1, Section 1.2
- Emphasizes foundation vs wiring metaphor
- Specific hardware requirements with examples
- Common failure modes and solutions
////
