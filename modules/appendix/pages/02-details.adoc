= Course details


== Timing and schedule

**Module 1**::
MaaS Architecture (30 minutes)
**Module 2**::
MaaS Configuration (40 minutes)
**Module 3**::
Model Deployment to the MaaS catalog (55 minutes)
**Module 4**::
MaaS Governance, observability, and chargeback (50 minutes)
**Module 5**::
API integration and consumer experience (40 minutes)
**Break time**::
10 minutes between modules recommended


== Technical requirements

=== Software versions

* Red Hat OpenShift 4.20 or later
* Red Hat OpenShift AI 3.3 or later
* OpenShift AI operator channel: `stable-3.x`
* NVIDIA GPU operator (latest/stable)
* OpenShift Service Mesh operator (required for Gateway API)
* Web browser (Chrome, Firefox, Safari, Edge)

=== Lab Environemnt Suggestions

**Lab Env**: A Single Node OpenShift (SNO) cluster with multiple NVIDIA GPUs:

* **Minimum GPU configuration**: 1x NVIDIA GPU with 24GB VRAM
* **Recommended GPU configuration**: 2x NVIDIA GPUs with 24GB+ VRAM each
* **GPU models supported**: A10, A100, L40S, L4, H100 (NVIDIA certified accelerators)
* **Purpose**: Deploy multiple models simultaneously (Llama Stack, Nemotron) with distributed inference

**Why multiple GPUs matter for this lab**:

* LlamaStack model is required for MaaS Dashboard (consumes 1 GPU)
* Shows realistic multi-tenant model serving scenarios
* Enables proper load balancing and autoscaling demonstrations
* Deploy the NVIDIA-Nemotron-3-Nano-4B-FP8 (or 30B A3B)


== Environment setup

=== Pre-QuickCourse checklist

The following components should be pre-configured before the QuickCourse starts:

□ **OpenShift cluster deployed** - SNO with GPU nodes configured

□ **GPU operators installed** - NVIDIA GPU operator and Node Feature Discovery

□ **OpenShift AI operator installed** - Channel: stable-3.x

□ **Service Mesh operator installed** - Required for Gateway API routing

□ **Red Hat Connectivity Link Operator Installed** - Kaudrant - Authentication/Authorization/Rate Limiting

□ **Leader Worker Set Operator installed for llm-d** - Required for Multiple node Inference (MOE)

□ **Web Terminal Operator or Developer Spaces** - Required CLI access

□ **KServe components deployed** - Model serving infrastructure ready

□ **GPU driver verification** - NVIDIA drivers loaded and tested

□ **OpenShift CLI available** - `oc` command accessible from terminal

////
=== Setup validation

Participants should run these commands at the start of the QuickCourse to verify environment readiness:

Check OpenShift login:
[source,role="execute"]
----
oc whoami
----

Verify OpenShift AI operator:
[source,role="execute"]
----
oc get csv -n redhat-ods-operator | grep rhods
----

Check GPU nodes:
[source,role="execute"]
----
oc get nodes -l nvidia.com/gpu.present=true
----

Verify GPU operator pods:
[source,role="execute"]
----
oc get pods -n nvidia-gpu-operator
----

Check KServe components:
[source,role="execute"]
----
oc get pods -n redhat-ods-applications | grep kserve
----

== Troubleshooting guide

=== Common setup issues

**Problem**: "oc command not found"

→ **Solution**: Verify terminal session is connected to bastion host, `oc` should be pre-installed

**Problem**: "Unable to connect to OpenShift cluster"

→ **Solution**: Verify cluster ingress is accessible, check Showroom environment variables are populated

**Problem**: "OpenShift AI operator not found"

→ **Solution**: Check operator installation in `redhat-ods-operator` namespace: `oc get csv -n redhat-ods-operator`

**Problem**: "GPU nodes not detected"

→ **Solution**: Verify GPU operator installation and node labels: `oc describe node | grep nvidia`

**Problem**: "KServe pods failing to start"

→ **Solution**: Check Service Mesh operator is installed, verify knative-serving namespace exists

=== During QuickCourse support

* Encourage participants to use Showroom split view (console + terminal side-by-side)
* OpenShift AI dashboard tab provides GUI alternative for most tasks
* Have backup cluster access credentials ready for authentication issues
* Use OpenShift web terminal as fallback for SSH connectivity problems
* Monitor GPU utilization to prevent resource exhaustion: `oc adm top nodes`

////

== Follow-up resources

=== Next steps for participants

* link:https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4[Red Hat OpenShift AI Documentation^, window=blank]
* link:https://developers.redhat.com/products/red-hat-ai[Red Hat Developer - OpenShift AI Resources^, window=blank]
* link:https://access.redhat.com/articles/rhoai-supported-configs-3.x[Red Hat OpenShift AI 3.x Supported Configurations^, window=blank]
* link:https://www.redhat.com/en/technologies/cloud-computing/openshift/openshift-ai[Red Hat OpenShift AI Product Page^, window=blank]


== Authors and contributors

**Primary Author**::
Karlos Knox +
Content Service Architect

**Last Updated**: April 2026

**QuickCourse Version**: 3.3

**Based on**: Red Hat OpenShift AI 3.3+ MaaS capabilities

**Contact Information**:

* QuickCourse feedback: Submit issue via Github repository [fix_me_link]
* Content updates: GitHub issues on QuickCourse repository (link provided by facilitator)

**Acknowledgments**: This QuickCourse incorporates SME guidance from Red Hat OpenShift AI business unit and product engineering architects working with enterprise AI deployments.

* Taylor Jordan Smith
* James Harmison 
